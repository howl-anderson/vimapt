packages:
    - "mkl-11.0.0"
    - "numpy-1.7.0; depends (mkl >= 10.3.0)"
    - "nomkl_numpy-1.7.0; provides (numpy == 1.7.0)"
    - "scipy-0.12.0; depends (numpy >= 1.7.0)"

installed_repository:
    - "nomkl_numpy-1.7.0"

remote_repository:
    - "mkl-11.0.0"
    - "numpy-1.7.0"
    - "nomkl_numpy-1.7.0"
    - "scipy-0.12.0"

request:
    - operation: "install"
      requirement: "scipy"
