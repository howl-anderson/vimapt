packages:
    - "mkl-10.3.0"
    - "numpy-1.7.0; depends (mkl)"
    - "numpy-1.8.0; depends (mkl)"
    - "scipy-0.12.0; depends (numpy >= 1.7.0, numpy < 1.8.0)"
    - "scipy-0.13.0; depends (numpy >= 1.8.0, numpy < 1.9.0)"
    - "scikit_learn-0.12.0; depends (numpy >= 1.7.0)"
    - "sklearn-0.13.0; replaces (scikit_learn <= 0.12.0); depends (numpy >= 1.7.0)"

installed_repository:
    - "mkl-10.3.0"
    - "numpy-1.7.0"
    - "scikit_learn-0.12.0"

remote_repository:
    - "mkl-10.3.0"
    - "numpy-1.7.0"
    - "numpy-1.8.0"
    - "scikit_learn-0.12.0"
    - "scipy-0.12.0"
    - "scipy-0.13.0"
    - "sklearn-0.13.0"

request:
    - operation: "install"
      requirement: "scipy"
